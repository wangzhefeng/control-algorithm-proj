# 项目上下文

## 仓库目标

- 这是一个控制算法学习项目，重点在 `src` 目录下的代码学习、修复与实验。

## 工作边界

- 不处理 `docs` 目录下文件。
- 优先修复可运行性问题，再做结构优化。
- 修改后要给出最小可复现实验步骤。

## 技术栈

- Python（由 `uv` 管理依赖）
- 部分 Matlab 示例

## 依赖约定

- 若 Python 代码缺失依赖，使用 `uv add <package>` 添加。
- 保持 `pyproject.toml` 与 `uv.lock` 一致。
